# v7.429 validation

109 available Python regression scripts passed; Logos lint and probe-helper shell syntax passed. The historical command-handoff assertion initially detected a missing validate step in COMMANDS.md; the guarded validation step was restored and that check rerun successfully.

New compact-stripe test executes the actual menu TWB and cleanup JavaScript at strengths 0, 45 and 100, checks repeated injection, verifies one banner owner and leaf media resets, and matches the probe-observed hp-stripe hierarchy while rejecting unrelated stripe links. Historical TWB golden checks remove only the two exact approved selector additions before comparison.

Probe evidence: main-document hp-stripe > hp-lucid-wrapper > a.stripe, 398x54 CSS px, blue rgb(1,120,250), filter:none. Its stripe-media-container contains playing video with readyState4, natural378x168, rendered126x54, filter:none. The separate ape_gateway_atf_mshop_iframe is not this banner.

Runtime delta is two selector additions in the existing menu TWB sheet. Configured brightness factor is reused; existing preference refresh/cleanup and document-load injection are unchanged. No new observers, timers, probes, or media-playback operations.

No iOS SDK/Theos/device available; visual validation pending. test_v7385_sponsored_c_linkage.py is excluded because clang is unavailable.
