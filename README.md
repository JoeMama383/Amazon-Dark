# AmazonDark v7.404

Product Search polish on top of v7.403. VIDEO_SINGLE_PRODUCT keeps one outer gray frame around the combined video + product-copy card instead of nesting a second border around the text half. The probe-proven Alexa prompt uses the standard AmazonDark dark-gray control fill, gray edge and white text while preserving the authored Alexa icon. All v7.403 Share/probe-control work remains.

All universal probes are regenerated for v7.404. See `COMMANDS.md`.
