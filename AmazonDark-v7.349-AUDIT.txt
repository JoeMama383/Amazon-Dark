AmazonDark v7.349 — persistent Cart shimmer-border strip fix
============================================================
Base: exact v7.348 source.

Probe + screenshot proof:
- Screenshot white band is exactly 39 physical pixels on a 3x screenshot = 13 pt.
- Dominant band pixel is RGB (234,237,237).
- At the screenshot-triggered Cart loading state, the v7.348 Web recorder captured:
  #sc-recs-atf-shimmer-placeholder
  rect = 430x328
  background = rgb(0,0,0)
  border-top-width = 13px
  border-top-color = rgb(234,237,237)
- This exactly matches both the visible thickness and pixel color.
- At USER_MARK, the native AWLoadingIndicatorBarView had already left the window, so the persistent screenshot strip is not the native 5pt bar.

Fix:
- Existing exact shimmer-placeholder rule now also forces border-color/outline-color OLED black.
- Exact #sc-recs-atf-shimmer-placeholder top/bottom border colors are explicitly black.
- Border width/geometry is preserved; only paint changes.
- v7.348 native loading-gradient fix is retained unchanged.

Runtime cost: static document-start CSS only. No new observer, timer, RAF, scan, hook, or listener.
