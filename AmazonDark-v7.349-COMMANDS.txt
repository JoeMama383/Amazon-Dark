PUSH
====
D=/private/var/mobile/Containers/Shared/AppGroup/D846D8DE-EE0F-4B82-9676-C68769E519CD/Documents
cd /var/mobile/Amazon-Dark-phone && git checkout -q main && git fetch origin main && git merge --ff-only origin/main && T=/var/mobile/t7349 && rm -rf "$T" && mkdir -p "$T" && unzip -q "$D/AmazonDark-v7.349-cart-shimmer-border-strip-fix-source.zip" -d "$T" && find . -mindepth 1 -maxdepth 1 ! -name .git -exec rm -rf {} + && cp -a "$T/AmazonDark-v7.349-cart-shimmer-border-strip-fix-source/." . && grep -qx 'Version: 7.349~cart-shimmer-border-strip-fix' layout/DEBIAN/control && grep -q '#define AD_VERSION "v7.349-cart-shimmer-border-strip-fix"' src/Tweak.xm && grep -q 'sc-recs-atf-shimmer-placeholder{border-top-color:#000' src/Tweak.xm && git diff --check && git add -A && git status --short && git commit -m 'v7.349: fix persistent Cart shimmer border strip' && git push origin main

VERIFY PROBE (optional; no further probe is required to identify the owner)
=======================================================================
cd /var/mobile/Amazon-Dark-phone && sh scripts/skeleton-probe.sh arm transition
# Reproduce Cart refreshes. Expected: 13pt white band is gone.
cd /var/mobile/Amazon-Dark-phone && sh scripts/skeleton-probe.sh export
